<html>
<head>
    <title>Sites BigCorp</title>
    {{> partial-import}}
</head>
<body>
{{> partial-header}}
<div class="container">
    {{#site.id}}
    {{#captor.id}}<h1>Modification capteur fixe</h1>{{/captor.id}}
    {{^captor.id}}<h1>Création capteur fixe</h1>{{/captor.id}}

    <form action="/sites/{{site.id}}/captors/FIXED" method="post">
        <div class="form-group">
            <label for="siteName">Site</label>
            <input type="text" readonly class="form-control" id="siteName"
                   value="{{captor.site.name}}">
        </div>
        <div class="form-group">
            {{#captor.id}}<input type="hidden" class="form-control" name="id" value="{{captor.id}}">{{/captor.id}}
            <label for="captorName">Capteur</label>
            <input type="text" class="form-control" id="name" name="name" required
                   {{#captor.id}}value="{{captor.name}}"{{/captor.id}}
                    {{^captor.id}}placeholder="{{captor.name}}"{{/captor.id}}>
            <label for="defaultPowerInWatt">Puissance par défaut</label>
            <input type="number" class="form-control" id="defaultPowerInWatt"
                   name="defaultPowerInWatt" required
                   value="{{#captor.defaultPowerInWatt}}{{captor.defaultPowerInWatt}}{{/captor.defaultPowerInWatt}}"
                   aria-describedby="nameHelp" placeholder="Puissance par défaut (en W)">
            <small id="nameHelp" class="form-text text-muted">Le capteur renverra toujours la
                même puissance égale à cette valeur saisie</small>
        </div>
        <a class="btn btn-secondary" href="/sites/{{site.id}}">Annuler</a>
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{#captor.id}}
            <button type="button" class="btn btn-danger" data-toggle="modal"
                    data-target="#deleteModal">Supprimer
            </button>
        {{/captor.id}}
    </form>
    {{/site.id}}
    {{^site.id}}
        <p>Problème lors de la lecture du capteur</p>
    {{/site.id}}
</div>
{{#captor.id}}
    <div class="modal" tabindex="-1" role="dialog" id="deleteModal">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirmation suppression</h5>
                    <button type="button" class="close" data-dismiss="modal" arialabel="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Etes vous sûr de vouloir supprimer le capteur <b>{{captor.name}}</b> ?</p>
                </div>
                <div class="modal-footer">
                    <form action="/sites/{{site.id}}/captors/FIXED/{{captor.id}}/delete" method="post">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close
                        </button>
                        <button type="submit" class="btn btn-danger">Supprimer</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{{/captor.id}}
{{> partial-footer}}
</body>
</html>